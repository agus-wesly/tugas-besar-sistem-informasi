import * as admin from './admin.server'
import * as user from './user.server'
import * as menu from './menu.server'
import * as pesan from './pesan.server'

export const model = {
  admin,
  user,
  menu,
  pesan,
}
